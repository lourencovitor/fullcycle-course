
class Video{
  private type: string;

  public calculateInterest(){
    if(this.type == 'Movie'){
      // calculate interesse based in film
    }else if(this.type == 'TVShow'){
      // calculate interesse based in series
    }
  }
}